=============================
Java Chatroom
=============================

A chatroom with a server and a client in the command line. Also has client commands.

To launch:

Create a server by launching Server.java in cmd.

Then create a client by launching Client.java in cmd and connect to the server using its IP address.

Can open multiple clients.